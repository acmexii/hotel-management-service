apiVersion: v1
kind: Service
metadata:
  name: checkinout-management
  labels:
    app: checkinout-management
spec:
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: checkinout-management