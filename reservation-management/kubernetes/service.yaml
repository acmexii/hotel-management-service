apiVersion: v1
kind: Service
metadata:
  name: reservation-management
  labels:
    app: reservation-management
spec:
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: reservation-management